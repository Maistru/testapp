import 'package:flutter/material.dart';

class PageViewTow extends StatefulWidget {
  const PageViewTow({Key? key}) : super(key: key);

  @override
  State<PageViewTow> createState() => _PageViewTowState();
}

class _PageViewTowState extends State<PageViewTow> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.deepPurple,
      ),
    );
    
  }
}